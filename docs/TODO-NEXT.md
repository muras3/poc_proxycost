# 次にやること（保留中のタスク）

いま動いているタスクが片付いたあとに着手する。**忘れないためにここに置く。**

---

## 1. 発生が確実なのに `—` にしている費目を、値か推定で出す

**優先度: 高。プロダクトの定義に直接反する。**

### 何が問題か

プロダクトの定義は「**実際に取引する総額に限りなく近い値を、できる限り正確に出す**」。
推論は推論と分かる形で出せばよい。**本当は必要なものを出さないのは誤り。**

`docs/DESIGN-NOTES.md` §2 の2軸分類が最初からこう決めている:

| | 必ず発生 | 条件付き・任意 |
|---|---|---|
| 金額が確定 | 商品代・代行手数料 … | 保護梱包・特殊梱包 … |
| **金額が推定** | **国内送料・国際送料・VAT** | 梱包の重量加算 |

**「発生は確定・金額が推定」は推定として出す。** これが仕様。
`—`（tier `none`）を使ってよいのは**発生しないもの**だけ。

**実装がここから外れている。**「持っていない数字は `—`」を、発生が確実なものにまで
機械的に当ててしまっていた。

### 実測（2026-09-07、1点 ¥5,000・500g・ヤフオク × 7カ国）

`—` の費目が **28個**。種類は5つ:

| 費目 | 発生 | いま | あるべき |
|---|---|---|---|
| Customs clearance fee（DE/FR/AU/SG） | **確実に発生** | `—` | 値を取る。取れなければ推定＋estimate。**CA は T23 で解消**（Canada Post CAN$9.95 の原文が取れた。C$20 以下は 0 と原文が書いているので、その帯の 0 は取得できた 0） |
| ~~Provincial tax（CA）~~ | **確実に発生** | ~~`—`~~ → **数値（T23 で解消）** | 州選択と人口加重の代表値。Canada Post CAN$9.95 も同時に入った |
| Duty（CA） | 発生する（C$20 超・品目次第） | `—` | 品目分類が要る。T24 の範囲 |
| US import prepayment（Zonos） | 発生する帯がある | `—` | 額を取りに行く。無理なら推定 |
| 前徴収 GST（SG の Neokyo/ZenMarket/Jauce） | **確実に発生**（徴収者が代行か税関かが不明なだけ。**払う額は同じ**） | `—` | 推定を出す |
| Sales tax / VAT（US） | **発生しない**（連邦の売上税は無い） | `—` | **これは正しい。触るな** |

米国（USPS $9.35）・英国（Royal Mail £8）・カナダ（Canada Post CAN$9.95、T23 で取得）は
既に値が入っているので、通関手数料は残り4カ国（DE / FR / AU / SG）ぶんを同じ形で取ればよい。
**カナダで通ったやり方が使える**: 郵便事業者の customs 案内ページを当たると、額と
「いくら以下なら課されないか」が同じ1枚に載っていることがある。

### やり方の要点

- **まず一次情報を取りに行く。** 各国の郵便事業者（Deutsche Post/DHL、La Poste、
  Australia Post、Canada Post、SingPost）が公表している輸入時手数料。名称は国ごとに違う
  （Auslagepauschale / frais de dossier / Import Processing Charge / handling fee）
- 取れたら tier `fixed` ＋ 出典URL ＋ 確認日
- **どうしても取れないものだけ推定にする。** その場合 tier を `estimate` にして
  画面で `~` と琥珀色に出し（`src/lib/ui/tiers.tsx`）、**推定の根拠を note とコメントに書く**
  （何を根拠に、どういう計算で、どれくらいの幅か）
- **発生しないものを推定で埋めない。** 米国の連邦売上税は `—` のままが正しい

### 検証で見るべきこと

1. `—` の費目を全部列挙し、1つ1つ「本当に発生しないのか」を判定する
2. tier が `estimate` なのに根拠が書かれていないものを洗う
3. 発生しないものが数字になっていないか（米国の連邦売上税が数字なら重大違反）

---

## 2. 料金表のデータ化（`docs/DESIGN-FEES-DATA.md` の移行6段）

設計は確定済み（コミット `8248249`）。実装が未着手。

利用者の判断が要る点が5つあり、うち2つは 1. の方針変更で扱いが変わる:

| # | 論点 | 状態 |
|---|---|---|
| 1 | サービス料が `unknown` の行を順位から外すか | **`unknown` を作らないのが方針。**20サイト×5社の料率を取り切る。取れないサイトは検索対象から外す |
| 2 | その社がそのサイトを扱わないときの見せ方 | 末尾に理由つきで残す（推奨） |
| 3 | Jauce の `other` / `amazon-jp` / `mercari` の料率 | 原文に記載が無い。**`unknown` ではなく取りに行く** |
| 4 | `Fees checked on` を最古の日にする | やる（推奨） |
| 5 | 監視先を `/ja/fees.aspx` から引用元の `/en/fees.aspx` へ | 寄せる（推奨） |
