# proxycost — 総額算出ロジックの検証計画

対象：Buyee / ZenMarket / Neokyo / FROM JAPAN / Jauce ／ 7カ国
作成 2026-09-07。すべての指摘は本リポジトリ HEAD（`src/calc.js` / `src/data.js`）で実行して確認した。

---

## 0. 「ロジックが正しいか」は5つの別の問いである

混ぜて測ると、誤差が出たときに原因を永久に特定できない。

| | 問い | 正解の出どころ | 実購入 | 現状 |
|---|---|---|---|---|
| **Q1** | 与えられた入力から、我々の規則どおりに計算しているか（実装の正しさ） | 手計算・期待値 | 不要 | **単体 536 件・e2e 214 件**（2026-09-08） |
| **Q2** | 我々の規則（料金表・EMS表・税率）が各社の公開料金と一致しているか（表の正しさ） | 各社の公式ページ・送料計算機・見積画面 | **不要** | **O1 完了／O2 は5社×1点** |
| **Q5** | **各社の料金項目が、国ごとに全て網羅されているか（漏れの正しさ）** | 各社の見積画面・実請求書への**逆向き照合** | ほぼ不要 | **`—` は3種のみ、3種とも正しく `—`**（2026-09-08。米国の連邦売上税・Zonos の非公表額・Neokyo が米国宛に日本郵便を売っていないこと）。**机上で潰せる穴は無くなった** |
| **Q3** | 予測時点で不明な入力（国内送料・梱包後重量）の仮定が実態と合うか | 実注文の倉庫到着時の画面 | 必要（**配達は不要**） | **0件** |
| **Q4** | 総額が実支払額と一致するか（Q1〜Q3の合成＋通関） | 配達完了後の全請求 | 必要 | **0件** |

**2026-09-07 追記。**上の「現状」列は判定日（2026-09-06）から動いた。**重いのは Q1 ではなく Q3・Q4。**
Q1・Q2・Q5 は実購入が要らないので机上で潰せるが、**Q3・Q4 は実購入でしか動かない。**

**Q1・Q2・Q5 は実購入ほぼ0件で終わる。**

> **判定日（2026-09-06）の原文（残す）:** そして Q1 のテストは現在1件も無い
> （`package.json` は `node --test 'test/**/*.test.js'` を実行するが `test/` が存在せず、
> `npm test` は `1..0` を返して**成功扱いになる**。README の「test/calc.test.js 7件」は事実と異なる）。

**2026-09-07: これは解消した。**`npm test` は Vitest で **536 件**、`npm run test:e2e` は
Playwright で **214 件**（desktop / mobile）を実行し、どちらも CI（`.github/workflows/ci.yml`）に入っている
（件数は 2026-09-08 時点）。
CI はさらに `master/validate.py`（実請求の再現）と `master/render-docs.py --check`（文書と JSON のずれ検出）も走らせる。

Q1・Q2・Q5 を飛ばして Q4 の実購入を15件やっても、誤差 12% が出たときに
実装バグなのか料金表の転記ミスなのか**費目の漏れ**なのか重量推定なのか通関なのか、切り分けられない。

**Q2 と Q5 は方向が逆であることに注意。**Q2 は「持っている数字が正しいか」（偽陽性を潰す）、
Q5 は「数字が全部揃っているか」（偽陰性を潰す）。Q2 が満点でも Q5 が抜けていれば総額は必ず過小になる。
**実購入は Q3・Q4 のためだけに使う。**

---

## 1. オラクル（正解の出どころ）の階層

| 層 | 出どころ | 取れる粒度 | 購入 | 対象 |
|---|---|---|---|---|
| O1 | 各社の公開料金ページ | 手数料の額と課金単位 | 不要 | Q2 |
| O2 | 各社の公開送料計算機 | 重量 × 国 → 国際送料 | 不要 | Q2 |
| **O3** | **各社のカート／見積画面（決済前）** | **代行側の手数料と国内送料の実額のみ**（国際送料・税は出ない社がある） | **不要** | **Q2 ＋ Q5（代行側だけ）** |
| O4 | 倉庫到着後の見積画面 | 実測重量・三辺・parcel数・確定国内送料 | 必要（配達不要） | Q3 ＋ Q5 |
| O5 | 配達完了後の全請求 | 総額**と全明細行** | 必要 | Q4 ＋ Q5（国側） |

**O3 は 1円も払わずに、同一入力に対する各社の明細行を取得できる。**
「代行側の手数料が合っているか」に対する、最も直接的で最も安い答えはここにある。

> ## **2026-09-08 訂正。O3 で取れる範囲を、実測より広く見積もっていた。**
>
> ここには元「5社とも、決済ボタンを押す前に**手数料・国内送料・想定送料**の明細が出る」
> 「O3 で確定できないのは**実測重量（O4）と通関・輸入税（O5）の2つだけ**」と書いてあった。
> **ZenMarket で実際にやってみたら、想定送料は出なかった。**
>
> ZenMarket のカート明細の列は原文で
> `Products / Qty / Price / Shipping within Japan / ZenMarket fee / Item ID / Coupon code`
> の**7列だけ**。**配送方法のセレクタも宛先国のセレクタもカート画面に存在しない。**
> 国際送料・輸出通関手数料・代理徴収税・配送方法の選択・宛先国の切替は、すべて
> **「代金を払って倉庫に商品が届いた後」の parcel 作成画面にしかない**
> （`PARCELS > CREATE A NEW PARCEL` は
> 「You don't have any items arrived yet to create a parcel」を返す）。
>
> **つまり ZenMarket では、国際送料と税は O3 ではなく O4 でしか取れない。**
> 我々が「1円も払わずに取れる」と数えていたものの一部が、**実購入の側に移る。**
>
> **これは1社の観測で、5社の結論ではない。**FROM JAPAN は見積の段階で国際送料を
> 出すので、カート段階でも出る可能性がある（未確認）。
> **次に確かめるべきはそこ**（`docs/O3-CART-RUN.md`）。
>
> 代わりに **O2 が持ち上がった。**`docs/audit/o2-courier-2026-09-08.md` のとおり、
> ZenMarket の公開見積 API は**購入もログインも無しに、全便の国際送料を返す。**
> 国際送料について O3 に期待していたことは、**O2 のほうが安く広く取れる。**

O3 で確定できないもの ── **実測重量（O4）**、**通関・輸入税（O5）**、
そして**社によっては国際送料そのもの（O4）**。

---

## 2. 総額ではなく「明細行」で突き合わせる

`calc.js` は `lines[]` を持っている。総額だけを比較すると、
**+¥500 の誤りと −¥500 の誤りが相殺されて「合っている」ように見える。**
15件の総額比較で誤差 3% という結果が出ても、それが「2つの大きな誤りの相殺」である可能性を排除できない。

行単位で突き合わせる。我々の label と各社画面の項目名の対応表を先に作る：

| 我々の label | Buyee | ZenMarket | Neokyo | FROM JAPAN | Jauce |
|---|---|---|---|---|---|
| Items | ○ | ○ | ○ | ○ | ○ |
| Purchase fee / Service fee | ○（per order） | ○（per item） | ○（per item） | ○（per item） | ○（価格連動） |
| Domestic handling | ○ | — | — | — | — |
| Domestic shipping | ○ | ○ | **要確認** | ○ | ○ |
| Packing | 任意 | 任意 | ○（段階） | 任意 | ○ |
| Deposit fee | — | ○（%） | — | — | ○ |
| EMS | ○ | ○ | ○ | ○ | ○ |

1:1 で対応しない箇所（Buyee の per-order 分割、Neokyo の梱包料の段）は
**明示的にグループ化してから**比較する。グループの定義そのものが検証対象。

判定：
- **公開料金表に由来する行** → 完全一致（1円でも違えば不合格）
- **推定に由来する行**（国内送料・国際送料） → 誤差率を記録し、行ごとに追跡

---

## 3. Q1：単体テスト（実購入0件・今日できる）

### 3.1 すでに確認済みの欠陥（4件）

テストを書く前に、コードを動かして以下を再現した。**いずれも実購入なしで見つかる。**

**① EMS 表の上限超過が黙って過小評価になる**（`data.js:29-32`）

```
emsStepIndex(20000) = 26 = EMS_TABLE の最終 index
→ 20 kg の荷物が "zone 4, 1 parcel, ~15.0 kg" として ¥39,100 で計上される
```

`findIndex` が -1 のとき最終行を返すため、**表の範囲外がエラーにならず、15kg 料金に丸められる。**
表示ラベルも「~15.0 kg」と嘘をつく。範囲外は `null`＋警告にすべき。

**② 未取得（`null`）が総額から静かに落ちる**（`calc.js:122`）

```
DE 単品: Items=3000 | Service fee=350 | Domestic shipping=0 | Packing=500
       | EMS=3650 | Duty=489 | VAT=1508 | Customs clearance fee=null
total = 9497   ← 通関手数料が「—」として合計から消えている
```

`lines.reduce((a, l) => a + (l.amount || 0), 0)` が `null` を 0 として扱う。
画面は「—」と正直に出すが、**総額は必ず過小になる。**
`calc.js:115` の入金手数料の課税ベースも同じ式なので、ZenMarket の手数料も連動して過小になる。
「未取得を0と書かない」という `data.js` 冒頭の設計方針が、**総額の側で破れている。**

**③ 「注文」の数え方が商品数になっている**（`calc.js:70-76`, `120`）

```
2店舗3点 / Buyee default → parcels = 3, "¥500 × 3 orders", 通関手数料 $9.35 × 3
```

`items.length` を注文数として使っている。**item に `seller` フィールドが存在しない。**
同一店舗の2点は Buyee では1注文＝1 parcel になるはずで、
購入手数料・国内配送サービス料・parcel 数・通関手数料が**すべて過大**になる。
DE/FR の `flatDutyPerItem × n` も同じ `n` を使っている。
検証計画の「組C：2店舗から合計3商品」は、**まさにこの欠陥を踏む構成**。

**④ 内部値が文言に漏れる**（`calc.js:34`, `data.js:107`）

```
SG: {"label":"Duty","amount":0,"note":"under the SGD Infinity threshold"}
```

`dutyFreeLimit: Infinity` がそのまま利用者向けの文言に出る。

### 3.2 書くべきテスト

| 群 | 内容 |
|---|---|
| EMS | 全27段の境界（`w`, `w+1`）× 7ゾーン。**表の上限超過**。0g・負値 |
| 手数料境界 | 点数1/2/N、注文数、Buyee の per-order 分割、`stepOffset` ±1 |
| 入金手数料 | `base / (1 - r) - base` の逆算（`10000 / 0.965 = 10363` が実請求と1円差で一致：`DESIGN-NOTES.md` §3） |
| 閾値 | `dutyFreeLimit` / `vatFreeLimit` を跨ぐ1円差、両側。`Infinity` と `0` の特殊値 |
| null 伝播 | **未取得が総額に混入しない／黙って落ちない**こと。②の回帰テスト |
| 課税ベース | CIF と FOB の切り替え、VAT が duty を含むか、parcel 数の掛かり方 |
| 不変条件 | 総額 = 行の和（null を除外した旨が明示されること）／ 全行が非負 ／ 商品数を増やすと総額が単調増加 |

---

## 4. Q2：表の突合（実購入0件）

**4.1 O1 — 公開料金ページ vs `data.js`**
5社 × 全費目を1つの表に落として突き合わせる。GitHub Actions の週次差分検知
（`REQUIREMENTS.md` §4）は**この表が正しいことを前提にしている**ので、初回は手で全件照合する。

**4.2 O2 — 各社の送料計算機 vs 日本郵便の公表表**
重量 × 国の格子を各社の計算機に入れ、我々の表と突き合わせる。
- 完全一致 → 国際送料の誤差要因は**重量推定だけ**に絞れる（Q3 の設計が単純になる）
- 不一致 → 各社が独自マージン／独自便を持っている。社別係数としてモデル化する

**この突合をやらずに実購入すると、国際送料の誤差が「重量推定の誤差」に誤って帰属される。**

### 4.2 の結果（2026-09-07・フェーズ1／`docs/O2-CALCULATOR-RUN.md`）

**ドイツ宛 600g で5社を実測。18観測中16が1円まで一致。**

**① EMS は5社とも公表額と完全一致**（全社 ¥3,400）。
→ 「各社は公表額をそのまま転嫁している」という仮定の裏付けが取れた。
確度を Buyee `unverified` / ZenMarket・FROM JAPAN `estimate` → **全社 `fixed`** に上げた。
**総額の 8〜24% がこの仮定に乗っていた。**

**② 上乗せは「社ごと」ではなく「方式ごと」だった。**

| 社 | EMS | 上乗せのある方式 |
|---|---|---|
| ZenMarket | 一致 | 小形包装物(航空) **+45.2%**（¥2,047 対 公表 ¥1,410） |
| Jauce | 一致 | 国際小包(船便) **+10.0%**（¥2,750 対 公表 ¥2,500） |

社に1つの係数では表せないので `Service.postage`（方式ごと）に変えた。
**率か定額かは観測1点では決められない**（+¥637 は 2kg で ¥4,567 と ¥5,706 に分岐）ので、
率として持ち確度は `estimate`。**フェーズ2（重量を変えた観測）で決まる。**

**③ 品揃えが社で全く違った。**FROM JAPAN 5方式 / Buyee・ZenMarket 4 / Neokyo 3 / **Jauce 2**。
全社が全方式を出す前提だったのは誤り。Neokyo・Jauce は小形包装物を売っていない。

**④ 既定方式に業界標準は無い。**FROM JAPAN だけが最安を自動選択、Buyee は EMS に
`Recommended` バッジ（選択済みではない）、3社は既定選択なし。
**EMS を既定にしている社は無い。**それでも既定を EMS に据え置いたのは、実請求の収集
（`research/real-invoices.md`）で船便が約40言及中3件しかなく、最安（＝たいてい船便）を
既定にすると実際にはほぼ使われていない方式の額が見出しになるため。

**残り: フェーズ2**（7カ国 × 3重量）。これで②の率か定額かが決まり、Q2 の O2 が終わる。

**4.3 O3 — 各社のカート／見積画面 vs `lines[]`**
同一商品URL・同一配送先で、5社すべてのカートに入れて決済直前の明細を取得する。
§2 の対応表で行ごとに比較。**これが Q2 の最終判定。**購入は発生しない。

ここで拾えるもの：
- Neokyo の ¥350 に国内送料が含まれるか（**`data.js:37` の `domesticIncluded: true` は現在まったくの未検証で、
  かつ `DESIGN-NOTES.md` §1 によれば1位が Neokyo → FROM JAPAN に入れ替わる唯一の条件がこれ**）
- ZenMarket の入金手数料が支払方法でどう変わるか（現行 `depositRate: 0.035` 固定）
- FROM JAPAN の手数料が定額か率か（現行 `perItemVerified: false`）
- Buyee が同一店舗の複数点を1注文と数えるか（欠陥③の答え）
- Jauce の価格連動手数料の実際の刻み

---

## 5. Q5：費目の網羅性（会社 × 国 × 条件）

**Q2 と方向が逆。**Q2 は「持っている数字が正しいか」、Q5 は「数字が全部揃っているか」。
**欠落は必ず過小方向に出る。**そして利用者が困るのは過小予測だけ。

そして **無いものはテストできない。**「知らない費目が無いこと」を直接テストする方法は存在しない。
→ **逆向きに照合する**（§5.5）。これが unknown unknown を見つける唯一の手段。

### 5.1 現状の計測（実行して確認）

`calc.js` が全社 × 全7カ国 × 3構成で出しうる費目ラベルは **14種**
（うち VAT / GST / Sales tax は同一スロット）＝ **実質 11 スロット**。

一方 `REQUIREMENTS.md` §1.4 は「現時点で19項目を把握」と書き、
`DESIGN-NOTES.md` §2 と `REQUIREMENTS.md` §8.5 に列挙された費目を数えると **28項目**ある。

→ **「把握している費目」と「計算に入っている費目」の間に、既に2倍以上の差がある。
実購入0件で今日測れる。**

未実装のうち、任意オプションではなく**必須かつ閾値付き**のもの：

| 費目 | 条件 | 現状 |
|---|---|---|
| FROM JAPAN Export Clearance Fee ¥2,800 | 商品代 20万円超 | **未実装。**`src/*.js` の `2800` は EMS 表の値としてしか存在しない |
| FROM JAPAN Payment Fees Inside Japan ¥200 | 常時（要再確認） | 一次情報で取得済み（`REQUIREMENTS.md` §8.5）だが未実装 |
| Buyee 通関手数料 ¥2,800 | 条件不明 | `DESIGN-NOTES.md` §2 は任意扱い。必須かどうか要確認 |

**閾値付きの費目は平均誤差率に現れない。**特定の価格帯だけで階段状に外れるので、
サンプルが安価な商品に偏っていれば**最後まで発見できない**。
検証計画の組A（¥1,000）・組B（¥5,000）・組C（合計¥7,000）は、
**20万円の閾値を1件も跨がない。**この構成では ¥2,800 の欠落は原理的に検出できない。

### 5.2 網羅性は「空欄禁止」でしか測れない

現行の `null` は **「この会社/国では発生しない」と「我々が調べていない」を区別できない。**
しかも欠陥②で、**両方が同じく 0 として総額から落ちる。**
→ **網羅性が構造的に計測不能になっている。**まず3値に分ける：

```js
{ value: 2800, source: 'https://...' }          // 金額あり（出典必須）
{ notApplicable: true, source: 'https://...' }  // 発生しない（出典必須）
{ unknown: true }                                // 未取得 → 区間の上端に算入し、警告を出す
```

**未定義（空欄）はビルドエラーにする。**費目カタログ × 会社 × 国 の全セルを埋めることを強制する。
これをやらない限り「網羅されているか」という問いに機械的な答えは出ない。

### 5.3 軸は「会社 × 国」だけではない

現在の入力は `items{ price, weight, freeShipping, domesticShipping }` と `country` だけ（実行して確認）。
**条件付き費目を表現するフィールドがそもそも存在しない：**

| 軸 | 変わる費目 | 現在の入力 |
|---|---|---|
| 支払方法 | 入金手数料（ZenMarket「1%から」）、コンビニ払い ¥1,000 | **無い** |
| 配送方法 | 国際送料、燃油・遠隔地サーチャージ、DDP 可否、保険 | **EMS 固定** |
| 仕入先 | ZenMarket の ¥800 / ¥500（Mercari・JDirectItems vs 一般店舗） | **`seller` が無い**（欠陥③と同根） |
| 商品属性 | リチウム電池・危険物・大型・要検品・再梱包 | **無い** |
| 申告額 | 免税閾値、輸出通関手数料 ¥2,800（20万円超） | `price` はあるが閾値ロジックが無い |
| 郵便番号 | CA 州税、遠隔地料金 | **無い** |
| DDU / DDP | 立替手数料、DDP 手数料、Zonos | **無い** |
| 数量 | 点あたり手数料 | **`qty` が無い**（`items` 配列の長さで代用） |

**網羅性はデータの問題ではなくスキーマの問題。**
入力に無い軸の費目は、どれだけ調査しても表現できない。
**Q5 の最初の作業は費目調査ではなく入力スキーマの拡張。**

### 5.4 網羅表は3枚作る

**表A：費目カタログ（正規化辞書）**
`費目ID / 各社の表記ゆれ / 課金単位（点・注文・parcel・%・重量段） / 必須か条件付きか / 依存する軸 / 出典`
表記ゆれの吸収がここの本体。同じ費目が社ごとに違う名前で出る。

**表B：会社 × 費目**（5社 × N費目、空欄禁止）

**表C：国 × 輸入側費目**（空欄禁止）
`関税 / VAT・GST / 州税 / 通関手数料 / 立替（advancement）手数料 / 配送業者取扱料 / DDP 手数料 / 低額貨物制度`

表Cは**「国だけ」では決まらない。国 × 配送業者 × DDU/DDP の3次元。**
同じ国でも郵便と DHL で行が違い、金額も課金単位も違う。
7カ国 × 主要2〜3業者 = 実質 14〜21 行。
現行 `COUNTRIES` は国ごとに `clearanceFeePerParcel` を1つしか持てず、**この軸を表現できない。**

### 5.5 逆向き照合（unknown unknown を見つける唯一の手段）

O3（決済前のカート画面）と O5（実請求書）に出た**全行**を、表Aの費目IDへ写像する。

- **写像できない行 = 網羅性の欠陥**（我々が存在を知らなかった費目）
- 表Aにあるのに画面に出ない = 過大計上の疑い、または発生条件が違う

判定：**未写像の行 0 件。**これは総額の一致とは**独立の**合否基準。
**総額が合っていても未写像の行があれば不合格**（相殺しているだけなので、条件が変われば崩れる）。

O3 は購入0件でできる。**5社 × 3構成 = 15画面を取るだけで、会社側（表B）の網羅性はほぼ確定する。**
国側（表C）は O5 が要るので、他人の請求書の収集が主な供給源になる。

### 5.6 網羅性の合格基準

| 基準 | 判定方法 |
|---|---|
| 表B・表Cに空欄が無い（3値のいずれかで埋まっている） | ビルドで強制 |
| `unknown` の件数と、それが総額に与えうる幅 | **画面に必ず表示。**0円として黙って落とさない |
| O3 / O5 の全行が費目IDへ写像できる | **未写像 0 件** |
| 必須・閾値付き費目の閾値の両側にテストがある | 全件（20万円 ±1円 など） |
| 条件軸の全値に対して費目が決定する | 支払方法 × 配送方法 × 仕入先 の直積で `unknown` が残る箇所を列挙 |

---

## 6. Q3：予測時点で不明な入力の検証（実購入は必要、配達は不要）

倉庫到着後・国際発送前の画面に、**実測重量・三辺・parcel数・確定国内送料**が出る。
ここで止めれば、配達と通関を待たずに最大の誤差源を測れる。

| 測るもの | 予測値 | 実測値 | 現行の仮定 |
|---|---|---|---|
| 国内送料 | 出品ページの表示 or 仮定 | 確定額 | `ASSUMED_DOMESTIC_SHIPPING = 800`（実勢 ¥150〜1,500 の中の一点） |
| 梱包後重量 | `商品重量 × 1.2 + 300g` | 倉庫の実測 | `PACKING_MULTIPLIER = 1.2` / `PACKING_ADD_G = 300`（`data.js` 上で仮定と明記） |
| parcel 数 | `parcelDefault` | 実際の分割 | Buyee のみ per-order（欠陥③） |

さらに **「実測重量を入れた国際送料」vs「予測重量を入れた国際送料」**を計算すれば、
**重量の誤差が送料の誤差に何円化けるか**を直接測れる。
`research/README.md` はフィギュアで重量の中央誤差 54% → 9% の改善余地を既に示しているので、
ここが最大の投資対効果。

**リードタイムが数週間短い**（配達・通関待ちが無い）ので、反復が速い。

---

## 7. Q4：総額の end-to-end（実購入・配達完了）

Q1〜Q3 が通ってから。測る対象は**通関・輸入税だけ**に絞られている。

正解＝同じ注文に帰属する全支払 − 返金。未使用残高は費用に数えない（入金時に失った手数料と、
その注文で消費した残高だけを配賦）。

**判定通貨は円。**カード発行会社の為替上乗せ（実勢 1.5〜3%）は利用者ごとに違い、
proxycost の管轄外。請求通貨で判定すると**測っているのは利用者の銀行であってモデルではない。**
請求通貨での差は「FX帯」として別掲する。

**商品代が大きいほど誤差率は良く見える。**主判定は総額で行いつつ、
`総額 − 商品代`（＝モデルが実際に予測した部分）の誤差を必ず併記する。

### 開始国は米国ではなく SG / AU

`data.js` の `COUNTRIES` を、通関の不確実性で分類する：

| 国 | 状態 | Q4 の適性 |
|---|---|---|
| SG | 関税0・GST 9%・免税 S$400 | **ほぼ決定的。Q3 の誤差を混入なしで測れる** |
| AU | 免税 A$1,000 以下なら関税不要・GST 10% | **ほぼ決定的** |
| DE / FR | €150 未満なら決定的。通関手数料が配送業者依存で `null` | 中 |
| GB | £135 超の `dutyRate: null` | £135 未満のみ |
| CA | 州税未取得・関税率 `null` | **郵便番号入力が必要。今の「国だけ」の入力では不能** |
| US | 関税率 12.5% が `dutyVerified: false`、通関 $9.35 が `clearanceVerified: false`、de minimis 停止 | **点予測不能。区間のみ** |

**検証計画の「全社 EMS・米国」は順序が悪い。**
米国から始めると、誤差が出たときに重量が悪いのか関税が悪いのか分離できない。
決定的な国で Q3 を確定させてから、不確実な国へ広げる。

### 点ではなく区間で評価する

米国のように点予測が構造的に不能な国がある以上、MAPE 一本では
「当たらない国では何も出せない」という結論しか出ない。

| 指標 | 定義 | 目標 |
|---|---|---|
| 絶対誤差率 | `\|予測 − 実額\| ÷ 実額`（円） | 中央値 ≤ 8%（n が足りるときのみ主張） |
| **カバレッジ** | 実額が80%予測区間に入った率 | **75〜85%**（狭すぎるのも過大主張として不合格） |
| 区間幅 | 区間の広さ ÷ 実額 | 小さいほど良いがカバレッジ優先 |
| pinball loss | 分位点損失（10/50/90%） | モデル改善の目的関数 |
| **過小予測** | `実額 − 予測 > 0` の大きさ | 利用者が困るのは過小だけ。**非対称に重く罰する** |
| 出力からの脱落 | 「未取得」として総額から落とした必須費目 | **0件**（欠陥②の回帰） |

カバレッジで評価すれば、米国でも「¥38,000〜46,000、10件中8件はこの範囲」と正直に出せる。

---

## 8. 何件で「ある程度正しい」と言えるか

Clopper-Pearson 95% 片側下限：

| 観測 | 観測合格率 | 真の合格率の95%下限 |
|---|---:|---:|
| 2/3 | 66.7% | **13.5%** |
| 3/3 | 100% | 36.8% |
| 13/15 | 86.7% | 63.7% |
| 15/15 | 100% | 81.9% |
| 26/30 | 86.7% | 72.0% |
| 85/100 | 85.0% | 77.8% |

- 検証計画の**「各社3件のうち2件以上が±10%」は、真の合格率 13.5% でも通る。**事実上の無検定。
- 「15件で15%超の過小予測が0件」でも、真の失敗率は**最大 18.1%** まで許容される（rule of three）。
- n=15 の中央値の95%CIは**4番目〜12番目の順位統計量**、つまり分布のほぼ全域。
  「誤差率中央値8%以下」を n=15 で判定する分解能は無い。
- 「±10%以内が80%以上」を主張するには**全件合格でも n≥15**、現実的な合格率85%なら **n≈150**。
- **国別**に主張するなら国ごとに上表が要る。7カ国 × 30件 = 210件。

→ **n=15 の正しい位置づけは合否判定ではなく「符号の大きい欠陥の探索」。合格基準を書いてはいけない。**
そして Q1・Q2 で見つかる欠陥（§3.1 で既に4件）は、n=15 の探索より安く確実に見つかる。

実購入のサンプル数を制約するのは費用：米国宛15件で **¥176,299**（現行4社・proxycost 自身の見積）。
7カ国 × 30件は約 ¥300万。**「各国で」の主張は、どの現実的な購入計画でも支えられない。**
国別は §7 の区間＋カバレッジで出す。

---

## 9. 予測の固定と、校正／検証の分離

購入・入札の**前**に予測を固定し、後から変更できないようにする。

```jsonc
// prediction/<order_key>.json — 購入前に確定。以後変更禁止
{
  "order_key": "2026-09-14-A-neokyo-SG",
  "frozen_at": "2026-09-14T03:11:22Z",
  "code_commit": "561f3c2",              // git commit = 改竄不能な時刻証明
  "rates_as_of": "2026-09-06",           // data.js RATES.asOf
  "input": {
    "items": [{ "url": "...", "price": 1000, "qty": 1, "seller": "...",
                "listed_domestic_shipping": 0, "free_shipping": true,
                "category": "カード", "predicted_weight_g": 50 }],
    "country": "SG", "postal_code": "...",
    "service": "neokyo", "shipping_method": "EMS",
    "consolidation": "none", "optional_services": [],
    "payment_method": "visa_credit", "billing_ccy": "SGD"
  },
  "output": {
    "total_jpy": 7278, "interval_80_jpy": [6100, 9400],
    "lines": [ /* calc.js の lines をそのまま */ ],
    "rank": 1,
    "excluded_unknown": []               // null で総額から落ちた費目。空であること（欠陥②）
  },
  "hash": "sha256:..."
}
```

```jsonc
// actual/<order_key>.json — 段階ごとに追記
{
  "order_key": "2026-09-14-A-neokyo-SG",
  "stages": {
    "cart":      { "lines": [ /* O3: 決済前の画面 */ ] },
    "charge1":   { "jpy": 1350, "billed": { "ccy": "SGD", "amount": 11.6 }, "evidence": "..." },
    "warehouse": { "measured_weight_g": 180, "dims_cm": [24,17,3], "parcels": 1,
                   "domestic_shipping_jpy": 0, "packing_jpy": 500 },   // O4: Q3 の正解
    "charge2":   { "jpy": 3900, "components": { "intl_shipping": 3400, "packing": 500 } },
    "import":    { "jpy": 0, "carrier": "SingPost", "terms": "DDU" }   // O5: Q4 の正解
  },
  "refunds": [], "unused_balance_jpy": 0,
  "actual_total_jpy": 5750,
  "complete": true    // 証拠が1つでも欠ければ false → 「正解データ不完全」として別集計
}
```

**校正と検証の分離**：`prediction/` を書いた後にモデルを変えた注文は、その時点で**校正データに降格**する。
合否に使えるのは「現行の計算規則で予測を固定し、その後に実額が確定した」注文だけ。
`code_commit` が現行 HEAD の祖先かどうかで機械的に判定できる。

---

## 10. 実行順

| # | やること | 購入 | 対象 | 理由 |
|---|---|---|---|---|
| P0 | **§3.1 の欠陥4件を潰す**（EMS上限・null脱落・注文の数え方・Infinity文言） | 0 | Q1 | 全部再現済み |
| P0 | 単体テストを書く（現在0件） | 0 | Q1 | 以後の全検証の前提 |
| P0 | `item` に `seller` を追加し、注文数と商品数を分離 | 0 | Q1 | 欠陥③。「組C」がこれを踏む |
| P0 | **`null` を3値化**（`value` / `notApplicable` / `unknown`、空欄はビルドエラー） | 0 | **Q5** | **これが無い限り網羅性は計測不能**（§5.2） |
| P1 | O1：5社の公開料金 vs `data.js` 全件照合 | 0 | Q2 | 週次差分検知の前提 |
| P1 | O2：各社送料計算機 vs `EMS_TABLE` の格子突合 | 0 | Q2 | 国際送料の誤差を重量だけに絞る |
| P1 | **O3：5社のカート明細を決済前に取得し、行ごとに突合** | **0** | Q2 | **Neokyo の国内送料込みの真偽がここで決まる** |
| P1 | Jauce を `SERVICES` に追加 | 0 | Q2 | **現行コードに Jauce が無く、5社検証は今のままでは走らない** |
| P1 | **表A 費目カタログを作る**（5社の表記ゆれを費目IDへ正規化） | 0 | **Q5** | 逆向き照合の前提。実質11スロット vs 把握28項目（§5.1） |
| P1 | **入力スキーマの拡張**（支払方法・配送方法・仕入先・数量・郵便番号・DDU/DDP） | 0 | **Q5** | **軸が無い費目は調査しても表現できない**（§5.3） |
| P1 | **必須・閾値付き費目の実装**（FROM JAPAN 輸出通関 ¥2,800 / ¥200、Buyee ¥2,800 の要否） | 0 | **Q5** | 20万円超で階段状に外れる。現行の組A/B/Cでは検出不能 |
| P1 | **表B（会社×費目）・表C（国×配送業者×DDU/DDP × 輸入側費目）を空欄禁止で埋める** | 0 | **Q5** | 「国ごとに網羅」の本体 |
| P2 | **O3 / O5 の全行を費目IDへ逆写像**。未写像 0 件を合格基準にする | 0〜 | **Q5** | unknown unknown を見つける唯一の手段（§5.5） |
| P2 | 予測固定の仕組み（§9） | 0 | Q3/Q4 | 無いと以後の実購入が全部無効になる |
| P3 | SG または AU で 5社 × 3構成 = 15件、**倉庫到着まで** | 15 | Q3 | 重量・国内送料の実測 |
| P4 | 重量モデルを Q3 のデータで校正（`research/` の手法を全カテゴリへ） | 0 | Q3 | 最大の誤差源 |
| P5 | 同じ15件を配達完了まで追う → 米国・EU へ拡大、区間＋カバレッジで評価 | 少数 | Q4 | 通関 |

**P1 の O3 が本計画の要。**購入0件で、5社の明細行が実際に取れる。
`domesticIncluded` のような**順位を単独で決めている未検証の bool** は、ここで全部落ちる。

---

## 11. 現行コードの既知の差分

| 箇所 | 現状 | 問題 |
|---|---|---|
| `data.js:36-56` | `SERVICES` は4社。**Jauce が無い** | 5社検証が走らない |
| `data.js:37` | Neokyo `domesticIncluded: true` | **未検証。順位を単独で決めている**（O3 で確定できる） |
| `data.js:41` | ZenMarket `depositRate: 0.035` 固定 | 公式は支払方法依存（「1%から」） |
| `data.js:45` | FROM JAPAN `perItemVerified: false` | `REQUIREMENTS.md` §8.5 が一次情報で ¥500/点を取得済み → 反映漏れ |
| `data.js:29-32` | `emsStepIndex` が範囲外で最終行を返す | **欠陥①**：20kg が 15kg 料金で計上され、ラベルも「~15.0 kg」と嘘をつく |
| `data.js:70-72` | `ASSUMED_DOMESTIC_SHIPPING=800` / `PACKING_MULTIPLIER=1.2` / `PACKING_ADD_G=300` | Q3 の検証対象そのもの |
| `data.js:76` | `RATES` 固定 | `asOf` を予測レコードに必ず紐付ける |
| `data.js:107` | SG `dutyFreeLimit: Infinity` | **欠陥④**：`Infinity` が利用者向け文言に漏れる |
| `calc.js:115,122` | `l.amount \|\| 0` | **欠陥②**：`null`（未取得）が総額から静かに落ち、必ず過小になる |
| `calc.js:70-76,120` | `items.length` を注文数として使用。`seller` フィールドが無い | **欠陥③**：同一店舗の複数点で手数料・parcel数・通関手数料が過大 |
| `calc.js:49` | FOB国の VAT 課税ベースに `emsYen` を含む | AU/CA の課税ベースが実態と合うか未検証 |
| `calc.js` 全体 | 出力しうる費目は**実質11スロット** | `REQUIREMENTS.md` §1.4 の「19項目を把握」、実列挙では28項目に対し**2倍以上の欠落**（§5.1） |
| `src/*.js` | **輸出通関手数料 ¥2,800 が存在しない**（`2800` は EMS 表の値のみ） | 商品代20万円超で FROM JAPAN が必ず ¥2,800 過小（§5.1） |
| `data.js` `COUNTRIES` | `clearanceFeePerParcel` を国ごとに1つしか持てない | **国 × 配送業者 × DDU/DDP を表現できない**（§5.4 表C） |
| `compare()` の引数 | `items{price,weight,freeShipping,domesticShipping}` と `country` のみ | 支払方法・配送方法・仕入先・数量・郵便番号・DDU/DDP の軸が無い（§5.3） |
| `null` の意味 | 「発生しない」と「未取得」が同一表現 | **網羅性が構造的に計測不能**（§5.2） |
| `test/` | 存在しない | `npm test` が `1..0` で成功扱い |
| `README.md` | 「3社」「`src/fees.js`」「test/calc.test.js 7件」 | 全て現状と不一致 |

---

## 12. 検証計画の「要修正候補」の照合結果

HEAD に対して確認した：

| 指摘 | 実際 |
|---|---|
| 「ZenMarket 手数料を500円としている」 | **該当しない。**`data.js:41` は `perItem: 800`、`index.html:238` も「¥800 per item」 |
| 「FROM JAPAN に200円の国内決済手数料を加えている」 | **該当しない。**`calc.js` にも `index.html` にも ¥200 の加算は無い。`REQUIREMENTS.md` §8.5 の調査メモにあるだけで、計算には入っていない |
| 「ZenMarket 入金手数料 一律3.5%」 | **該当する。**`data.js:41` `depositRate: 0.035` |
| 「Neokyo の国内送料がサービス料に含まれると読める」 | **該当する。**含意ではなく `domesticIncluded: true` で実際に0円計上している |
| 「米国向け Zonos 等を金額不明として総額から落としている」 | **該当する。**しかも US 以外の `clearanceFeePerParcel: null` も欠陥②で総額から脱落しており、**影響は米国だけではない** |
