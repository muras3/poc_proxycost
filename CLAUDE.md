@AGENTS.md

## 開発の進め方（2026-09-11 確定）

オーナー1人が、コーディネーター役のセッションを通して複数のサブエージェントに作業を渡す形で開発している。次のセッションでも同じ形で動けるよう、ここに固定する。

### 1. 親セッションは作業をしない

オーナーと会話している親セッションは、**会話・設計判断・サブエージェントへの指示・PR のレビューだけ**を行う。
**実装・テスト・データ入力・文書の執筆・コミットはすべてサブエージェントに渡す。**

理由: 親セッションが直接ファイルを触ると、走っているサブエージェントとワークツリーを取り合う。また、親が作業を抱えると会話の応答が止まる。

### 2. サブエージェントは必ず独立した git worktree で走らせる

`Agent` ツールの `isolation: "worktree"` を必ず付ける。

理由（**2026-09-11 に実際に起きたこと**）: 2つのエージェントが同じワークツリーを共有し、互いのファイルを踏む危険が出た。一方が作った一時ファイル（`src/lib/pricing/tmp-rank-check.test.ts`）が、もう一方の作業と混ざった。コミット前の作業を退避するために、一時 index を使った plumbing（`git commit-tree` で HEAD と index を汚さずに退避ブランチへ push）が必要になった。`git pull` が「ローカルの変更で上書きされる」と言って失敗し、最新を取れなくなった。worktree を分ければ全部起きない。

### 3. `main` に直接 push しない。必ずブランチと PR

ブランチ名は `claude/<内容>`。PR を立て、**CI が全て green になってからオーナーがマージする。**サブエージェントはマージしない。

### 4. モデルの割り当て

| 役 | モデル |
|---|---|
| **設計判断・レビュー** | **Fable**（2026-09-16、オーナー指示で変更） |
| オーナーとの会話・サブエージェントへの指示・PRのレビュー | **Opus**（コーディネーター） |
| 実装・テスト・データ入力・調査 | **Sonnet / Opus**（規模と難度で選ぶ） |

**変更の経緯（2026-09-16）**: 以前は「Fable はオーナーが使うタイミングを指示する。勝手に使わない」だったが、
**設計判断は既定で Fable に出す**ことにした。この日、コーディネーター（Opus）が立てた「順位の不確実性表示」の
設計を Fable にレビューさせたところ、前提が2つとも壊れた——(1)「送料レンジが相関するので判別不能が過剰に出る」は
根拠の無い推測だった（レンジは社固有の実測点由来、`compare.ts` の `rankHighFor`）、(2) 見出しの根拠に
`computeBracket` の `recommended` を使う案は、**実データ392条件のうち69条件で3社目以降を黙殺**していた
（`BRACKET_CAP = 2` は判定ではなく画面の囲い数の上限、かつ重なり判定が推移的）。そのまま実装していれば出ていた。

Fable の使いどころ（`docs/FIT-GAP.md` の T-F0 の経緯として残っている）: **「検査が検査になっていない」型の欠陥を見つけるのに向く。**過去に、マスタを読まずに数値をベタ書きしていた検証器（書き換えても PASS していた）を Fable が見つけている。

### 5. 一時ファイルをリポジトリに作らない

デバッグ用・確認用のスクリプトやテストは、**リポジトリの中ではなくスクラッチディレクトリに置く。**やむを得ずリポジトリに作った場合は、**コミットする前に必ず消す。**

理由: 2026-09-11 に `src/lib/pricing/tmp-rank-check.test.ts` が作業ツリーに残り、PR に混ざる寸前だった。

### 6. CI の判定は `all green` ジョブだけを見る。シャードを個別に見ない

何が起きたか（**2026-09-12**）: あるPRで `lint / types / unit / master` ✅、`e2e 1/4` ✅、`e2e 2/4` ✅、`e2e 3/4` **cancelled**、`e2e 4/4` **cancelled** という結果が出た。コーディネーターは cancelled の2シャードを「まだ実行中」と読んで待ち、2回、合計およそ1時間40分を無駄にした。実際には何も動いていなかった。

重要な点: **リポジトリ自体のゲートは最初から正しかった。** `.github/workflows/ci.yml` の `all green` ジョブは

```
[ "$fast" = success ] && [ "$e2e" = success ] || exit 1
```

を実行しており、これは正しく `failure` を報告していた。不具合は判定ロジックではなく、**結果の読み方**にあった。個々のシャードを見てしまい、ゲートを見ていなかった。

したがって: **CI の合否は `all green` ジョブの結果だけで判断する。シャードを1つずつ確認しない。** `cancelled`・`skipped`・`timed_out` は合格ではない。一部のシャードが緑で一部が cancelled というマトリクスは、一見安心できるように見えて、実は e2e スイートの3分の1が実質未検証というだけのことがある。`cancelled` は流し読みすると「赤ではない」ように見える ── それこそが危険な理由だと覚えておく。

**追記（2026-09-12、run 34686078129 で判明した仕組み）**: 上のルールはまだ一歩足りなかった。`cancelled` という言葉自体が2つの別の原因を1つのラベルに押し込めている。

何が実際に起きたか: `e2e/assumed-weights.spec.ts:48` の `gotoCompare()` を呼ぶ最初のモバイル向けテスト以降、`[mobile]` の全テストが `workers: 1` で1つずつ、60秒のper-testタイムアウトで連鎖的に失敗し続けた。その連続失敗は、e2eジョブ自体の `timeout-minutes: 15`（`.github/workflows/ci.yml:51`）が15分13秒でジョブを打ち切るまで止まらなかった。**GitHub は `timeout-minutes` によってkillされたジョブも、concurrency groupに追い越された（supersede された）ジョブも、同じ `cancelled` として報告する。** これが罠の正体で、「別の実行に追い越されただけ、自分の問題ではない」と読めてしまうが、実際は「テストが延々と失敗し続け、持ち時間を使い切った」ことを意味する場合がある。（`ci-${{ github.event.pull_request.number }}` はPRごとに安定した一意の値になり、`deploy.yml` も別の concurrency group・別トリガーなので、このケースでワークフロー設定自体は原因から除外できた。）

したがって `cancelled` を見たら、チェック一覧だけで判断せず、**必ずジョブログを開いて次の2つの見分け方のどちらかを確認する**（チェック一覧の見た目ではこの2つは区別できない）:

- ジョブの `timeout-minutes` 付近で切れており、ログに実際のper-test失敗が並んでいる → **本物の失敗**。ログに名前が出ているテストがバグの所在。
- ジョブ開始からほぼ即座にキャンセルされており、同じPRに新しい実行が存在する → **本物のconcurrency supersede**。このときだけ無視してよい。

具体例（run 34686078129, 2026-09-12）: シャード `e2e 3/4` と `e2e 4/4` が `assumed-weights.spec.ts:48` 以降で連続タイムアウトし、15分13秒で `timeout-minutes: 15` に達してジョブが打ち切られ、`cancelled` と表示された。

もう1点: この種の「大量タイムアウトによる失敗」は**自己隠蔽的**である ── 壊すテストが多いほど、ジョブは（本物の失敗としてではなく）時間切れで死んで `cancelled` と自己申告する可能性が高くなる。

### 7. 新しい worktree の `node_modules` はリンクを張っただけでは信用できない。Turbopack はワークツリー外へのシンボリックリンクを拒否する

何が起きたか: 2つの別々のエージェントが Playwright をまったく実行できず、うち1つは e2e の変更をローカル未検証のまま出した。これが、9件の古びたフィクスチャがローカルで捕まらずCIまで届いた原因になった。当時は「新しい worktree には node_modules が無い」と原因を書いたが、これは誤りだった。**実際の原因は `cf4a8f0` で `node_modules` がリンク切れの（Mac のパスを指す）シンボリックリンクとして git に tracked commit されていたこと**で、`scripts/worktree-setup.sh` がメインチェックアウトの `node_modules` にリンクを張ろうとしても、リンク先自体が壊れたリンクだったため「リンクを張った」と成功を報告しながら依存が1つも解決できていなかった（tracked なので `.gitignore` の `node_modules/`（末尾スラッシュ付き）も効かず、checkout のたびに復活していた）。`git rm --cached node_modules` で tracking を外し、`.gitignore` に末尾スラッシュ無しの `node_modules` も追加し、`scripts/worktree-setup.sh` にリンク先の実在確認と `next --version` 実行による解決確認を足して直した（詳細は git log の tracking 除去コミットを参照）。

実際の手順を記録する。最初に思いつく回避策は非自明な形で失敗するため:

- 新しい worktree の `node_modules` は空。メインチェックアウトのものをシンボリックリンクする: `ln -s /home/user/poc_proxycost/node_modules <worktree>/node_modules`
- **すると Turbopack がビルドを拒否する**。「シンボリックリンクがファイルシステムのルートの外を指している」というエラーになる。**これは実測して確認済み**: リンク先が実体の（壊れていない）`node_modules` ディレクトリであっても、worktree の外を指すシンボリックリンクである限り同じ `TurbopackInternalError: Symlink [project]/node_modules is invalid, it points out of the filesystem root` で失敗する。つまり「リンク先がダングリングだから」ではなく、「シンボリックリンクで外を指していること自体」が原因。ローカルでは `next build --webpack` でビルドすること（実測でこちらは成功する）。これは**ローカル限定の回避策**であり、デフォルトのビルド設定を変更してはいけない。
- Chromium は導入済みで `PLAYWRIGHT_BROWSERS_PATH` も設定済み。**`playwright install` を実行しない**こと ── 不要な上にブロックされる。
- `playwright.config.ts` は以前 e2e のポートを `3100` に固定していた。並行して動く複数の worktree がこれに衝突する ── **2026-09-12、PR #85 の e2e がこれで一切動かず**、ポートを別の worktree に握られたままだったため、2つのシナリオを実行検証ではなくコード読解だけで済ませて出す羽目になった。今は `PORT` / `E2E_PORT` があればそれを使い、無ければ worktree の絶対パスから決定的にポートを導出する(メインチェックアウトと CI は常に `3100` のまま)。衝突時は「どのポートが」「別の worktree に握られている可能性が高い」と明示して落ちる。

上記を毎回書き起こさずに済むよう `scripts/worktree-setup.sh` を用意した。シンボリックリンクを冪等に張り、リンク先の実在とリンク切れ・自己参照でないことを確認し、張った後に `node_modules/.bin/next --version` を実際に実行して依存解決を検証してから成功を報告する。検証に失敗したら、何が壊れているかと次に打つべきコマンドを示して非ゼロで終了する。次に入るエージェントはこれを1回叩けばよい。

### 7-b. テストは並列が既定。単列に戻すのは、並列で壊れることを実測で示せたときだけ

**`playwright.config.ts` は `fullyParallel: true` / `workers: process.env.CI ? 2 : '50%'`。**単列（`workers: 1`）に戻してよいのは、**並列で壊れることを実測で示せたときだけ**で、「干渉しうる」という推測は理由にならない（本ファイル §9）。実測の根拠: 2026-09-07、4コア機・212件で `workers=1 → 4.9分 / 2 → 3.6分 / 4 → 3.0分`、**並列で落ちたテストは1件も無かった**。特定のファイルだけ直列が要るなら、config 全体ではなくそのファイルに `test.describe.serial` を付けて、なぜ要るかをコメントに残すこと。

### 8. 数値（合計・ランキング）を動かす変更と、UI を動かす変更は同じPRに混ぜない

何が起きたか: あるPRが (a) 3社で従来サイレントに欠落していた決済手数料の追加、(b) 配送業者の実測レートをランキングに組み込む変更、(c) 配送業者をUIに表示する変更、の3つを一度に運んだ。(a) がおよそ9件のe2eフィクスチャを無効にし、(c) がさらに2件とレイアウトのアサーションを壊した。両者が組み合わさった結果、レイアウトの回帰はフィクスチャの騒がしさの陰に隠れて、CIの2回目のラウンドまで表面化しなかった。

ルール: **合計やランキングを動かす変更は、UIを動かす変更とは別のPRで出す。**それぞれが単独でグリーンになるので、e2eが落ちたときにどちらが原因か一目でわかる。まとめて出すと、失敗が互いを覆い隠し合い、CIの1ラウンドごとに数分単位のコストがかかる。

同じ経緯から、もう一つ記録しておく価値がある: **バックグラウンドの待機には必ず期限を切る。**あるエージェントが期限を設けずにバックグラウンドのPlaywright実行を待ち、ターン終了まで何も得られなかった。フルマトリクスの数分の一の時間で1つの修正を検証するには、`-g "<test name>"` と単一プロジェクトへの絞り込みが効く。

### 9. 状況は理由にならない。理由には根拠が要る

何が起きたか（**2026-09-13**）: コーディネーターが「宅配便に `docs/DESIGN-BOX-SIZE.md` §2④（重量上限での箱分割）がなぜ繋がっていないか」を問われ、3回とも違う答えを口にした。

1. 最初は「原則としての決定」と答えた——実測レンジの天井を公表された上限として誤用しないための選択、と。**誤り**: `src/lib/pricing/postage.ts` の `maxGramsFor()` は `PostalMethod` しか受け取らず、`src/lib/pricing/` のどこにも運送会社の重量上限は存在しない。決定など無く、誰も調べていなかっただけ。
2. 次に、PR #109 が重量上限を収集した後、「運送会社は重量超過を拒否せず追加料金にするだけだから、分割してモデル化するものが無い」と答えた。**これも誤り**: 1社（DHL）の性質を4社に一般化していた。
3. オーナーがさらに2回押し返して初めて実際の根拠が出てきた: 4社中、**拒否しないと確認できているのは DHL だけ**（70kgでOverweight Pieceの追加料金、ネットワーク上限ははるかに上）。**UPS は国で割れている**——US/CA/AU/GBは追加料金だが、UPS Singapore自身のService Guideは「輸送として引き受けない (not accepted for transportation)」と明記。**FedExはデータが無い**（公式ページがHTTP 200でWAFページを返す）。**ECMSは自社の上限を公表していない。**

正直な現状は「宅配便の荷物が常に1箱に収まるという前提には根拠が無い」であって、コーディネーターは異なる言い回しでそれを2回、決定であるかのように語った。

ルール: **状況は理由にならない。**「我々のデータは20kgで止まっている」は今どこにいるかの記述であり、それより重い荷物が発生し得ないことも、運送会社がそれを受け付けることも証明しない。「なぜこれは扱われていないのか」と聞かれたら、答えは出典のある事実か「まだ確認していない」のどちらかでなければならない——決定めかした合理化であってはならない。

### 10. プロキシの計算機を測るときは、1条件ずつ・単一ブラウザセッションで、SUBMITを2〜3回押して値が固定されるのを確認する

何が起きたか（**2026-09-13、Jauceの再測定**）: PR #107 が記録した「AU/CA/FR/GBのEMSは重量に関わらず完全に平坦」という所見は、再測定でひっくり返った。同じ条件（20×15×10cm）で500g/3000g/20000gを1条件ずつ単一操作者・単一ブラウザセッションで測り直すと、EMSは¥3,150→¥8,800→¥44,500と明確に重量に応じて増加した。「平坦」に見えた原因は少なくとも2つ複合していたと判断している: ①SUBMITを押しても前条件の値が残留表示される、あるいは初回クリックでは結果テーブルがそもそも描画されない、というクライアント側の表示バグ、②複数の自動化タスクを同一Chromeプロファイル・同一タブグループで並列実行したことによる、タブ間の入力・結果の汚染（例: フランス20000gの結果に無関係な「+ GST for Australia」注記とオーストラリア特有の上限値が混入した）。

さらに悪いことに、陽性対照（US、手順が変化を検出できているかの確認用）ですら、3000g付近の値が前回¥4,180・再測定¥10,300と大きく食い違った。**同じ条件で2倍以上違う値が出た**ということは、前回roundのデータは「平坦に見えた4カ国」に限らず、全体として信頼性が疑わしいことを意味する。技術的な汚染経路（タブ間で具体的に何が共有されていたか）はここでは特定できていない——推測でこれ以上絞り込まない（本ファイル§9参照）。

ルール: プロキシの送料・手数料計算機を測定するときは、**1つの条件を、1つのブラウザセッションの中で、順番に**測る。**SUBMITは1回のクリックを信用せず、2〜3回押して同じ値が連続で出ることを確認**し、そのつど**画面に表示されている入力値（重量・寸法など）が実際に入力したものと一致しているか**を確認する。**同一のブラウザプロファイルを複数のエージェントで共有して並列実行しない。**「新規タブ＝真っさらな状態」という前提は、少なくともこの種の並列実行下では成立しない。

**追記（2026-09-13、PR #112・FROM JAPANの実測で判明）**: 上のルールは「価格の数値が安定するか」しか見ていない。PR #112 は同一条件（SG・500g）を2回測り、1回はUPSが¥2,891で表示され、もう1回はUPSがまったく表示されなかったことを記録した——これはJauceで見つかった2つの不安定さ（stale値の残留、初回クリックでの未描画）とは別の第3の形で、**価格ではなく「どの配送方法が表示されるか」という集合そのものが条件間で揺れる**。価格を2〜3回のSUBMITで固定しても、この種の揺れは検出できない：毎回同じ価格が出ていても、出てくる方式の顔ぶれが違うことがある。

したがってルールを拡張する: SUBMITを2〜3回押して確認するのは**価格だけでなく、表示された配送方法の集合（何が出て、何が出ていないか）**も含める。方式の顔ぶれが1回でも変わったら、それ自体を不安定性として記録し、価格が一致しているからといって信頼できると判断しない。2026-09-13だけで、2つの異なるプロキシの計算機から少なくとも3種類の不安定さ（stale値の残留／初回未描画／方式の出現消失）が見つかっている。3社目・4社目で新しい形が出る前提で測定すること。

### 11. コーディネーターはオーナーに短く答える（2026-09-16、オーナー指示）

親セッションの報告が長すぎる。以下を守る。

- **結論を先に、1〜2行で。**背景・経緯・気づいたことの列挙を頭に置かない。
- **表は「判断に要る列」だけ。**参考情報の列を足さない。
- **1回の返答で問える判断は原則1つ。**未回答の判断事項を毎回再掲しない（1回言えば伝わっている）。
- **サブエージェントの報告をそのまま転記しない。**オーナーの判断が変わる点だけ抜く。
- **自分の誤りの訂正は1行。**経緯の説明を付けない（このファイル冒頭の「Corrections」の方針と同じ）。

**ただし短さのために落としてはいけないもの**: 根拠（ファイル:行・実測値）、未確認であるという事実、自分が間違っていたという事実。§9 は短さに優先する。
