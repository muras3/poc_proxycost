# proxycost — 要件定義

最終更新 2026-09-06 / 名前・ドメインは仮

## 1. プロダクト定義

1. キーワードまたは商品URLから日本の商品を特定し、**購入代行の主要5社**で
   「自国に届くまでいくら払うか」を出す。
2. 対象は**既に代行を使っている人**。「代行とは何か」の説明は入れない。
3. **入口は検索。**キーワードから日本の主要ショッピングサイト10前後を横断して候補を出し、
   画像つきで選ばせる。選んだ価格は**参考値**として入り、編集できる。**URLを貼る道も残し、
   そちらは確定値**として扱う。どのサイトの価格かを常に明示する。
4. 費目は商品代・代行手数料・国内送料・梱包・国際送料・入金手数料・関税・VAT・
   通関手数料ほか、公開情報から拾える全て（現時点で19項目を把握）。
5. 比較単位は「会社」ではなく**「会社 × 小包状態」**。Buyee は既定で注文ごとに別送するため
   2列に割る。
6. **重量が分かるカテゴリは1つの総額、分からないカテゴリは重量の段ごとに複数の総額**を出す。
   1つの数字を押し付けない。
   （2026-09-06 改訂: 段ごとの総額は撤去。重量は常に推定値を既定で入れ、カートでその場で
   直せる欄にし、1位を決めている品を名指しする。押し付けないのは「数字を出さない」ではなく
   「出どころを書き、直せて、効く品を名指しする」で満たす。docs/UI-DESIGN.md §4）
7. 数字は**確定・推定・二次情報・未取得**の4段階で見た目を分ける。未取得は「—」と書き、
   0とは書かない。
8. **重量表を公開ページとして出す**（出典・件数・取得日つき）。計算の中身を全部見せる。
9. **順位は総額のみで決める。**報酬を払う社と払わない社を画面に書く。
10. 使うのは1回きりでよい。Reddit から来て、計算して、代行サイトへ飛ぶ。
11. **表示広告を出す。**比較表の中・横には置かず、結果の下に限る。広告が順位と無関係で
    あることを明記する。

## 2. 約束すること / しないこと

| | |
|---|---|
| 約束する | **持っている一次情報の範囲で最も正確な総額**（下の内訳）と、**入力した重量での**順位と差額（総額のみで決まり、報酬は一切効かない） |
| 約束しない | **実請求と一致すること**／**重量を外しても順位が動かないこと** |

**総額を可能な限り正確に出して順位づけする、がこのプロダクトの定義。**
約束できるのは「一次情報で取れる範囲での最良の総額」までで、**実額との一致ではない**。
代行5社の費目を実請求と突き合わせた検証は **0件**（`docs/TODO-NEXT.md` §「残っている差分」。
`docs/O3-PLAN.md` の手順が未実行）なので、実額と一致するとは書かない。

**総額の確度**（5点 × ¥3,000・600g/点・ヤフオク、7カ国それぞれの1位の行の合計。任意費目を除く）
の実測値・国別内訳は、**この節に手で書かない。**`README.md` の生成節（`measured-basket`。
生成元は `src/app/sources/measured.ts` の `CONFIDENCE_SPLIT_BY_COUNTRY` / `CONFIDENCE_TOTAL`、
`master/render-docs.py --check` が縛る）を参照。同じ数字を2箇所に手で書いた結果、
以前ここには ¥263,981・84%（¥221,079）という、当時の再測定からも既にずれていた値が
残っていた。

**ここには以前「総額の約半分が推論」と書いてあった。**それは 2026-09-06 の値
（米国で 47% / 44% / 9%、`docs/DESIGN-NOTES.md` §1）で、その後 EMS 行の確度を
「料金の確度／重量の確度」に分けたこと（`docs/COMPLETENESS.md` T16。EMS 料金は公表額で、
推定なのは引くのに使う重量のほう）と、米国の通関手数料 $9.35 が前払い帯で 0 になったこと
（`docs/TODO-NEXT.md` §3）で公表側に寄った。**いま推論なのは半分ではなく 16%**
（推定 14% ＋ 二次情報 2%）。

**「重量の推定を 1/3〜5倍 に外しても、7カ国すべてで順位は1つも動かなかった」という
以前の根拠は、2026-09-06 の再測定で否定された。**1位が入れ替わる重量（点数ごと）と、
既定の推定値（5点 × 600g）での国別 `rankStable` は、**この節に手で書かない。**
`README.md` の生成節（`weight-rank`。生成元は `src/app/sources/measured.ts` の
`CROSSOVER_G` / `RANK_STABILITY`）を参照。
**ここには以前「7カ国すべてで `rankStable=false`」と書いていたが、これは間違いだった。**
2026-09-06 の測定以降に費目の修正が重なって値が動いたのに、`rankStable` だけ測り直す
仕掛けが無く誰も測り直していなかった。
1,500g/点では1位と2位の差が **¥50**（Neokyo ¥52,886 / FROM JAPAN ¥52,936、0.1%）しかない。

したがって画面は `rankStable` をそのまま出し、動く条件では「重量の推定が外れると
1位が替わる」と書く。**「概ね頑健」とは書かない。**
実装の頑健性判定は **×1/3 と ×3** の2点（`src/lib/pricing/compare.ts`）。それ以上振ると
梱包後の重量が EMS 公表表を出て、「順位が変わる」ではなく「比べられなくなる」ため。
実測値は `docs/audit/measured-2026-09-06.md`、内訳は `docs/DESIGN-NOTES.md` §1。

## 3. 技術スタック

| | 選択 | 理由 |
|---|---|---|
| 言語 | TypeScript（strict） | |
| フレームワーク | Next.js 16 + React 19（App Router） | ielts-app と揃える |
| スタイル | Tailwind v4 | 同上 |
| テスト | Vitest / e2e は Playwright | 同上 |
| バリデーション | zod | 商品ページのパース結果に効く |
| Lint | ESLint（flat config） | 同上 |
| スクリプト | tsx、`scripts/` に置き `npm run <domain>:<action>` | 同上 |
| パスエイリアス | `@/*` → `./src/*` | 同上 |
| デプロイ | **Cloudflare Workers**（`@opennextjs/cloudflare` 経由） | Cloudflare が Pages から Workers へ寄せている |
| キャッシュ | Cloudflare KV | 同じURLを何度も叩かない。相手への礼儀でもある |
| データ | リポジトリ内の JSON（料金表・EMS表・重量表） | DB不要。料金の変更履歴が git に残る |
| 検索 | Brave Search API | 月$5クレジット ≒ 1,000検索まで無料、以降 $0.005/query |
| 監視 | GitHub Actions（週次） | 各社の公式料金ページの差分で issue を自動生成 |
| 分析 | Cloudflare Web Analytics | 無料・cookie不要 |
| LLM | **計算には使わない。カート構築の自動化にだけ使う** | 非決定性が致命的。価値の源が数字の正確さなので。総額・順位・差額を決める経路に推論 API を置かない。範囲の確定は `docs/FIT-GAP.md` §6 |

## 4. データ源と更新

| データ | 源 | 更新 |
|---|---|---|
| 代行各社の料金 | 各社公式ページ | 週次で差分検知 → issue → 手で更新 |
| EMS 料金 | 日本郵便 公表料金表（全42段・30kg まで） | 同上 |
| 各国の税・免税限度 | 各国税関・政府ページ | 同上 |
| 商品重量 | 海外発送する日本の小売の公開商品JSON | 継続的に拡充 |
| 商品価格 | 検索（参考値）／商品ページ（確定値） | 都度 |

重量データの注意：Shopify の `grams` は**送料計算用の入力値であって実測ではない**。
店が一律・帯別送料を使っていると擬似値（例：カメラ店で全件1,500g、楽器店でギター180kg）。
**値の分布（distinct数・丸い値の比率）で品質を自動判定してから採用する。**

現時点で実データが取れたカテゴリ：**フィギュア**（Solaris Japan 5,000件）、
**レコード・CD**（Snow Records、CD 80〜120g / LP 240〜300g）。
取れなかった：楽器・カメラ・書籍・漫画・ゲーム専業。

## 5. 収益

| 経路 | 条件 |
|---|---|
| ZenMarket アフィリエイト | ¥100/登録。自社・審査なし・即日 |
| Buyee / FROM JAPAN | ASP 経由（0.8〜5% / 約2%）。審査待ちあり |
| Neokyo | **報酬なし。**最もよく最安になる会社が払わない |
| 表示広告 | AdSense。RPM $1〜5。月$60 には 12,000〜60,000 PV/月 |

**順位は報酬額を一切参照しない。**どこが払っていてどこが払っていないかを画面に書く。

## 6. 法務・コンプライアンス

- **駿河屋の robots.txt は全面禁止ではない。**`User-agent: *` は `Allow: /` で、
  `Content-Signal: search=yes,ai-train=no,use=reference`。禁止されているのは AI 学習系
  クローラ（ClaudeBot / GPTBot / CCBot / Bytespider 等）のみ。**プロダクト自身のクローラは
  許可される。**（Claude が 403 を食らうのは ClaudeBot が名指しで禁止されているため）
- データ収集は robots.txt を尊重し、KV でキャッシュして再取得を避ける
- 重量は事実であり著作物ではない。著作権法30条の4（情報解析）にも該当
- 収集した生カタログは再配布せず、**そこから導いた集計のみを持つ**
- **プライバシーポリシーのページ**（AdSense 審査要件）
- **EU・英国向けの同意バナー**（対象国に DE / FR / GB を含むため必須）
- アフィリエイトリンクに `rel="sponsored"`、開示をフッターに明記

## 7. 誰が何をやるか

| やること | 誰が | いつ | 状態 |
|---|---|---|---|
| Cloudflare の API トークン発行 → GitHub Secrets に登録 | 本人 | 公開前 | 未。**ダッシュボードで GitHub を接続する方式はやめ、GitHub Actions からデプロイする**（`cloudflare-api` の MCP が OAuth 認可を要し、非対話セッションから認可できなかったため）。手順は `TODO-USER.md` |
| KV 名前空間の作成と `wrangler.jsonc` の id 差し替え | 本人 | 公開前 | 未。現在 `PLACEHOLDER_REPLACE_ME`。**入れるまでデプロイは通らない** |
| Brave Search API のキー取得 → `wrangler secret put BRAVE_API_KEY` | 本人 | 検索を出す時 | 未。**キーが無い間はキーワード検索が無効になり、URL 貼付と手入力だけになる**（実装済みの縮退動作） |
| ドメイン取得（Cloudflare Registrar なら DNS 設定不要） | 本人 | 公開直前 | 未 |
| ZenMarket アフィリエイト登録 | 本人 | いつでも | 未 |
| Buyee / FROM JAPAN の ASP 申請 | 本人 | 早めに（審査待ち） | 未 |
| AdSense 申請 | 本人 | コンテンツが載ってから | 未 |
| GitHub issue テンプレ / Twitter の窓口 | 本人 | 公開前 | 未 |
| 実装 | Claude | — | **完了。**計算エンジン（5社 / EMS 42段 / 7カ国 / 確度4段階 / 段ごとの総額 / 順位の頑健性）、画面（`/` `/weights` `/sources` `/privacy`、モバイル対応、同意バナー、広告枠）、検索（Brave・商品ページ取得・KV キャッシュ）。lint / typecheck / Vitest 197件 / `next build` が通る |
| データ整備 | Claude | — | **一部。**料金5社・EMS・7カ国は投入済み。重量は §8 を見ろ |

## 8. 未決事項

**データ・料金**

| | |
|---|---|
| 為替を誰がいつ転記し直すか | 固定値の運用が未定。`RATES_AS_OF` を更新する担当と頻度を決めていない |
| レコード・CD の n=0 | Snow Records の公表レンジ（CD 80〜120g / LP 240〜300g）を転記しただけで件数を保持していない。**再取得が要る** |
| 重量が取れないカテゴリ | 楽器・カメラ・書籍・漫画・ゲームは、商品ごとの重量を持つ公開カタログが見つかっていない（`/weights` に理由つきで明記済み）。計算機は 1,000 g を「仮置き」と名乗って入れ、その場で直してもらう。**データはでっち上げない**（`resolveWeight` は今もフォールバックを返さない） |
| 各社の国際送料マークアップ | Jauce のみ「EMS 公表額そのまま」を実測。他4社は 0 と置いた**推定**。上乗せの有無が未確認 |
| FROM JAPAN の決済手数料 ¥200 | 点ごとか注文ごとか、原文から読めない。注文ごとと解釈して `unverified` で描いている |
| ZenMarket の同梱挙動の公式原文 | 準一次のみ。Cloudflare 403 で原文未確認（挙動自体は他3社と同じ「貯めて1個口」型と強く示唆） |
| Jauce の入金手数料の掛かり方 | 「¥40 + 3.9%」。定額が先に乗り率が gross-up で効くと解釈しているが原文未確認 |
| Jauce の紹介報酬の有無 | 確認できていない。**払うと書けないので「払わない」扱い**にしている |

**Brave（2026-09-07 に本番の `/api/search` を 55 クエリ叩いて実測。`docs/audit/search-reality.md`）**

| | |
|---|---|
| $5/1,000 プランに storage rights が含まれるか | <https://brave.com/search/api/> に「結果の一部または全部を保存したいならば、保存権を明示的に付与するプランに加入する必要がある」とある（確認日 2026-09-07）。**Search プランがそれに当たるとは書かれていない。**いま `cache.ts` は 24h 保存している。**Brave に問い合わせが必要（未決のまま）** |
| EC の商品ページで product.price を返すか | **返らない。**55 クエリ 1,090 件で 0 件。しかも現在の Web Search のドキュメント本文に `product` という語が1度も出てこない（確認日 2026-09-07） |
| thumbnail を返すか | **41%（1,090 件中 451 件）**。ドキュメントには載っていないが実測では返る。0% のクエリが 8 つあった |

**設計・方針**

| | |
|---|---|
| 価格の誤差に対する順位の頑健性 | 未測定。頑健性を測ったのは重量のみ。価格は ZenMarket の 3.5% を通じてのみ順位に効くはずだが、**測ってから言う** |
| ばらつき 1.0x の重量を「確定」に昇格させるか | 現状は全て「推定」。`research/README.md` は「確定値として扱える」と「実測ではない」の両方を書いている |
| 利用規模の第2指標 | Trustpilot 件数のみで判断している。アプリDL数・SimilarWeb は未取得 |
| インバウンド人気カテゴリ トップ20 | 未調査。重量データを取りにいったのは6カテゴリ（フィギュア・レコード/CD・釣具・スニーカー・トレカ・中古ブランド）で、これは網羅ではなく「取れた順」 |
| UI の言語 | 現状は英語のみ。日本語併記の要否は対象利用者の言語分布次第 |
| 名前・ドメイン | `proxycost` は仮 |

UI 側の細かい未決（`/weights` に広告を置くか、総額の丸め単位など）は
`docs/UI-DESIGN.md` §9 にある。**モバイルの列問題は解決済み**（横スクロール表をやめ、
順位リスト＋行の展開。同 §3）。段表は 2026-09-06 に撤去（同 §4）。

## 8.5 調査で確定したこと（2026-09-06）

**代行5社**（Trustpilot のレビュー件数・評点、確認日 2026-09-06）:

| | 件数 | 評点 |
|---|---:|---:|
| Neokyo | 10,112 | 4.6 |
| ZenMarket | 9,530 | 4.0 |
| Buyee | 8,058 | 3.6 |
| FROM JAPAN | 2,093 | 4.4 |
| Jauce | 1,138 | 4.4 |

対象5社は **Buyee / ZenMarket / Neokyo / FROM JAPAN / Jauce**。
※ Trustpilot 件数は「レビュー収集の熱心さ」の代理でもあり、規模の指標としては偏る。
Buyee はヤフオク公式提携で客が来るためレビューを集める必要が薄い。

**FROM JAPAN の料金**（公式配信の翻訳ファイル `/translate/en_help.txt` から原文取得）:
Handling Fee **¥500/点**（`500 yen per item`）／Product Protection Plan ¥500/点／
**Payment Fees Inside Japan ¥200**／保管60日無料／Export Clearance Fee ¥2,800（20万円超）／
コンビニ払い ¥1,000／再梱包 ¥1,500〜／写真 ¥500(3枚)。
**「5%」「$50超10%」は別サービス FROM USA の料金で、日本商品には適用されない。**

**同梱の既定挙動**：Buyee のみ「既定で注文ごとに別送、申請すると無料で同梱」。
Neokyo（45日）・FROM JAPAN（60日、一次原文取得）・ZenMarket（60日、準一次）は
いずれも「貯めて選択 → 1個口」型。**Buyee だけが外れ値。**

**Brave Search API**（2026-09-06 の記載を 2026-09-07 に原文で取り直して訂正）:
`site:` 演算子、Goggles、`country=JP` / `search_lang=ja` は使える。
1クエリ最大20件（offset 0–9 ＝実質200件）。$5/1,000 requests、月$5クレジット、50 QPS。

**訂正**: `thumbnail` と `product.price` は「スキーマに存在する」と書いていたが、
現在の Web Search のドキュメント本文（全文取得）には `product` も `thumbnail` も
**1度も出てこない**。実測でも `product.price` は 1,090 件で 0 件、`thumbnail` は 41%。
**ドキュメントに書かれた検索演算子は `"..."` / `-` / `site:` / `filetype:` の4つだけで、
`OR` も括弧も書かれていない。**いま `siteFilter()` が投げている
`(site:a OR site:b OR …)` は**ドキュメントに無い形**（実測では効いている）。
クエリ長・演算子の個数の上限は公式には見つからなかった
（`/documentation/resources/search-operators` は 2026-09-07 時点で 404）。
`extra_snippets`（1件あたり最大5本の追加抜粋）は未使用。
出典: <https://api-dashboard.search.brave.com/app/documentation/web-search/responses>
／ <https://brave.com/search/api/>（いずれも確認日 2026-09-07）。詳細は
`docs/audit/search-reality.md`。

## 9. 工程と日数

| フェーズ | 中身 | 日数 |
|---|---|---|
| 0. 調査 | 代行5社の特定／カテゴリ20／FROM JAPAN の料金／同梱挙動／Brave の画像 | 1 |
| 1. 骨格 | 計算エンジンを5社に拡張、重量の段対応、確度4段階、TypeScript 化 | 1 |
| 2. 取得 | Brave 検索、商品ページ取得、KV キャッシュ、サイト判定 | 1.5〜2 |
| 3. 画面 | 1画面・横並び・重量表ページ・モバイル対応 | 1 |
| 4. 公開 | Cloudflare、ドメイン、アフィリエイト、監視 Action、プライバシーポリシー、同意バナー、AdSense 申請 | 1 |

**合計 5.5〜6日。**フェーズ0を省き、フィギュアとレコードだけで出すなら **4日**。
